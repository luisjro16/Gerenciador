<!DOCTYPE html>
<html>
<head>
  <title>Detalhes do Usuário</title>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
</head>
<body>
  <div class="container mt-4">
    <h1 class="text-center">Detalhes do Usuário</h1>

    <div class="card custom-card">
      <div class="card-body">

        <% if @user.access_level != 'admin' %>
            <div class="position-absolute top-0 end-0 p-2">
                <%= button_to @department, method: :delete, form: {data: { turbo_confirm: "Você tem certeza?" }}, style: "background: none; border: none; padding: 0; box-shadow: none;" do %>
                <%= image_tag "lixeira.png", alt: "Apagar", style: "width: auto; height: auto;" %>
                <% end %>
            </div>
        <% end %>

        <h5 class="card-title"><%= @user.name %></h5>
        <p class="card-text"><strong>Email:</strong> <%= @user.email %></p>
        <p class="card-text"><strong>Nível de Acesso:</strong> <%= @user.access_level %></p>

        <h6>Departamentos:</h6>
        <ul>
          <% @user.departments.each do |department| %>
            <li><%= department.name %></li>
          <% end %>
        </ul>

        <div class="mt-3 d-flex justify-content-center">
          <%= link_to 'Editar Perfil', edit_admin_user_path(@user), class: 'btn btn-primary' %>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
