<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Resend Confirmation Instructions</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
      body {
        background-color: #121212; /* Dark background for the entire page */
        color: #e0e0e0; /* Light text color */
        margin: 0;
        padding: 0;
        overflow-x: hidden; /* Prevent horizontal scrolling */
      }
      .login-container {
        max-width: 420px;
        margin: auto;
        padding: 20px;
        border: 1px solid #333; /* Dark border */
        border-radius: 8px;
        background-color: #1e1e1e; /* Dark form background */
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.5); /* Darker shadow */
      }
      .login-header {
        text-align: center;
        margin-bottom: 20px;
      }
      .login-footer {
        text-align: center;
        margin-top: 20px;
      }
      .form-control, .form-check-input {
        background-color: #333; /* Dark input background */
        color: #e0e0e0; /* Light input text color */
        border: 1px solid #444; /* Darker border */
      }
      .form-control::placeholder {
        color: #888; /* Placeholder color */
      }
      .btn-primary {
        background-color: #007bff; /* Primary button color */
        border: none;
      }
      .btn-primary:hover {
        background-color: #0056b3; /* Darker shade on hover */
      }
      .text-body-secondary {
        color: #888; /* Secondary text color */
      }
      .text-body-secondary a {
        color: #007bff; /* Link color */
      }
      .text-body-secondary a:hover {
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <main class="login-container mt-5">
      <div class="login-header">
        <%= image_tag "bandeira.png", width: "52%" %>
      </div>

      <h2 class="text-center">Reenviar e-mail de confirmação</h2>

      <%= form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post, class: 'w-100' }) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div class="form-floating mb-3">
          <%= f.email_field :email, class: 'form-control', id: 'floatingInput', placeholder: 'name@example.com', autofocus: true, autocomplete: 'email', value: (resource.pending_reconfirmation? ? resource.unconfirmed_email : resource.email) %>
          <label for="floatingInput">Email</label>
        </div>

        <div class="text-center">
          <%= f.submit "Reenviar ", class: 'btn btn-success w-100 py-2' %>
        </div>
      <% end %>

      <div class="login-footer mt-2 d-flex justify-content-start">
        <%= link_to "Entrar", new_session_path(resource_name), class: 'btn btn-primary me-2' %>
      </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
  </body>
</html>
